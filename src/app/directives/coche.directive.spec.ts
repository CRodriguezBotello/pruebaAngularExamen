import { CocheDirective } from './coche.directive';

describe('CocheDirective', () => {
  it('should create an instance', () => {
    const directive = new CocheDirective();
    expect(directive).toBeTruthy();
  });
});
